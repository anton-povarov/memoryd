# Domain docs

This is a single-context repository.

Before relevant work, read `CONTEXT.md` and applicable ADRs under `docs/adr/`. Proceed silently if either is absent.

Use glossary terminology and explicitly flag conflicts with existing ADRs.
